# CLAUDE.md - AI Assistant Guide for KiraEval

## Project Overview

KiraEval is a project in its initial bootstrapping phase. The repository currently contains only foundational files (README and LICENSE). This guide should be updated as the project structure evolves.

- **License:** Apache 2.0
- **Repository:** NotG8ENouGh/KiraEval
- **Default branch:** master

## Repository Structure

```
KiraEval/
├── CLAUDE.md       # This file - AI assistant guidance
├── LICENSE         # Apache License 2.0
└── README.md       # Project readme (placeholder)
```

## Current State

The project is in its initial state with no source code, build system, dependencies, or tests yet established. When these are added, this document should be updated accordingly.

## Development Workflow

### Git Conventions

- The default branch is `master`
- Feature branches should use descriptive names
- Commit messages should be clear and descriptive, summarizing the "why" of changes
- GPG signing is enabled for commits

### Before Committing

Since no linter, formatter, or test suite is configured yet, manually review all changes before committing. As tooling is added, update this section with the specific commands to run.

## Key Guidelines for AI Assistants

1. **Read before editing** - Always read a file before proposing changes to it
2. **Minimal changes** - Only make changes that are directly requested or clearly necessary; avoid over-engineering
3. **No speculative features** - Do not add features, abstractions, or utilities beyond what is asked for
4. **Update this file** - When adding new project infrastructure (build system, test framework, linting, CI, etc.), update this CLAUDE.md to reflect the new state
5. **Check for existing patterns** - As the codebase grows, follow established patterns and conventions rather than introducing new ones
6. **Security first** - Do not introduce common vulnerabilities (injection, XSS, etc.) and avoid committing secrets or credentials

## Technology Stack

Not yet established. Update this section when a language, framework, and tooling choices are made.

## Build & Run Commands

No build system configured yet. Update this section when build tooling is added.

<!-- Template for future use:
### Build
```bash
# build command here
```

### Test
```bash
# test command here (run all tests)
# single test command here
```

### Lint / Format
```bash
# lint command here
# format command here
```
-->

## Testing

No test framework configured yet. Update this section when tests are added.

## Environment Setup

No special environment setup required at this time. Update this section when dependencies or environment variables are introduced.
